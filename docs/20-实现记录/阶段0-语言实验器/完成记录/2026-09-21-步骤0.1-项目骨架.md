# 步骤 0.1：项目骨架和开发入口

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 创建最小 Go 模块；
- 创建中文命令入口；
- 增加帮助、版本和未知命令提示；
- 为三个入口行为添加测试；
- 建立阶段 0 示例和测试目录；
- 添加源码阅读说明。

## 没有做什么

- 没有读取中文源码文件；
- 没有定义 Token；
- 没有实现 Lexer、Parser、AST 或解释器；
- 没有引入第三方依赖。

## 验证

运行 `GOCACHE=/tmp/xu-go-cache go test ./...`，命令入口的三个测试通过；同时验证了版本和帮助命令的实际输出。

## 遗留问题

- Go 模块名 `example.com/zhongwen` 只是当前本地占位，项目名称确定后再调整；
- Go 包目录使用 ASCII 的 `cmd/zhongwen`，否则中文目录会被 Go 当作非法导入路径；最终命令显示仍使用中文；
- 测试曾因按字节截取中文字符串而失败，已改为按 UTF-8 字符串前缀判断；
- CLI 的命令名称和输出格式还没有冻结；
- 下一步需要先讨论 Token 的最小集合，再开始 Lexer。

## 下一步

进入阶段 0.2：定义最小 Token 类型、源码位置和打印方法。完成后再次暂停。
