# 步骤 0.10：语句边界实验

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 比较换行、缩进、分号和混合方案；
- 使用多条赋值、空行和文件末尾样例验证各方案的影响；
- 形成“换行为主、分号暂缓”的临时决定；
- 明确下一步只实现换行 Token，不增加表达式和其他语言特性。

## 没有做什么

- 没有修改 Lexer；
- 没有修改 Parser；
- 没有让一个文件支持多条语句；
- 没有加入分号、缩进 Token、括号和注释。

## 验证

现有代码保持不变，运行 `GOCACHE=/tmp/xu-go-cache go test ./...`，所有测试通过。

## 下一步

进入 0.11：Lexer 保留换行 Token，Parser 暂时只支持多条整数赋值。完成后暂停。
