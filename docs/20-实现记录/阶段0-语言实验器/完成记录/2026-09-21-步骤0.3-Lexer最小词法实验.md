# 步骤 0.3：Lexer 最小词法实验

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 创建 `internal/lexer`；
- 识别空白、换行、`设置`、中文标识符、ASCII 标识符、整数和等号；
- 记录 Token 的 UTF-8 字节偏移、行号和列号；
- 对无法识别的字符返回带位置的中文错误；
- 添加最小赋值、换行、关键字边界、英文标识符和错误位置测试。

## 没有做什么

- 没有实现 Parser；
- 没有实现表达式和语句；
- 没有读取 `.中` 文件；
- 没有增加完整关键字表；
- 没有加入文字、布尔、小数、括号和注释。

## 验证

运行 `GOCACHE=/tmp/xu-go-cache go test ./...`，所有测试通过。

## 遗留问题

- Lexer 的错误恢复策略尚未设计；
- 列号是否需要考虑终端显示宽度，暂时不处理；
- 后续增加关键字时，需要避免与标识符边界规则冲突。

## 下一步

进入阶段 0.4：定义最小 AST，并让 Parser 解析一条 `设置 名字 = 1`。完成后暂停。
