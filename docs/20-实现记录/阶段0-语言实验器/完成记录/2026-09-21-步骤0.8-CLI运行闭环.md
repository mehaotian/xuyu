# 步骤 0.8：CLI 最小运行闭环

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 创建 `internal/runner`，连接文件读取、Lexer、Parser 和 Interpreter；
- 为 CLI 增加 `运行` 命令；
- 支持运行一个 `.中` 文件并显示变量环境；
- 添加文件不存在、缺少路径、成功运行和未知命令测试；
- 更新阶段 0 示例和用户文档。

## 没有做什么

- 没有增加新的语言语法；
- 没有实现变量引用、表达式和输出语句；
- 没有实现项目目录、配置文件和多文件模块；
- 没有统一完整的诊断编号；
- 没有实现编译和发布。

## 验证

运行：

```bash
GOCACHE=/tmp/xu-go-cache go test ./...
GOCACHE=/tmp/xu-go-cache go run ./cmd/zhongwen 运行 examples/阶段0/最小赋值.中
```

实际运行结果：

```text
环境[名字=1]
```

## 遗留问题

- CLI 还不能显示 Token 和 AST 调试结果；
- 当前语言只能做一条整数赋值；
- 模块名和 CLI 最终命名仍是临时方案。

## 下一步

进入 0.9：阶段 0 结束评估，决定下一阶段先做变量引用、表达式，还是先改善 CLI 和错误显示。不在评估前继续堆叠语法。
