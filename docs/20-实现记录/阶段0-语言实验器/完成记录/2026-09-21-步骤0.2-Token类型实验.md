# 步骤 0.2：Token 类型实验

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 添加 `internal/source`，定义源码位置和范围；
- 添加 `internal/token`，定义六种最小 Token 类别；
- 为 Token 和源码范围提供可读的字符串表示；
- 添加类别、范围和 Token 输出测试；
- 记录 UTF-8 字节偏移和半开范围规则。

## 没有做什么

- 没有实现 Lexer；
- 没有读取源码；
- 没有处理完整关键字表、文字、括号或表达式；
- 没有修改 CLI 命令行为。

## 验证

运行 `GOCACHE=/tmp/xu-go-cache go test ./...`，所有测试通过。

## 遗留问题

- Lexer 需要决定中文列号按 Unicode 字符还是显示宽度计算；
- `无效` Token 是否保留到 Parser，需要在 Lexer 错误策略中确认；
- Token 类别仍会随着语言实验增加，但必须保持小步扩展。

## 下一步

进入阶段 0.3：只识别空白、一个中文关键字、中文标识符、整数和等号。完成后暂停。
