# 步骤 0.12：变量引用实验

**日期：** 2026-09-21
**状态：** 已完成

## 做了什么

- 增加 `VariableReference` AST 节点；
- Parser 支持整数常量或变量作为赋值右侧；
- Interpreter 按顺序读取变量环境；
- 未定义变量返回带源码位置的错误；
- 添加变量引用示例和完整链路测试。

## 没有做什么

- 没有实现算术表达式；
- 没有实现比较、布尔和文字；
- 没有实现类型检查；
- 没有实现作用域、函数和参数。

## 验证

运行：

```bash
GOCACHE=/tmp/xu-go-cache go test ./...
GOCACHE=/tmp/xu-go-cache go run ./cmd/zhongwen 运行 examples/阶段0/变量引用.中
```

预期输出：

```text
环境[名字=1, 总数=1]
```

## 遗留问题

- 变量引用当前只有整数一种值；
- 变量名解析仍然是运行时查找，不是静态检查；
- 下一步需要先决定最小算术表达式语法。

## 下一步

进入 M1：整数表达式闭环，一次完成四则运算、括号、优先级、变量运算和除零错误，不加入比较、布尔和文字。
