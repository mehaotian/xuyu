# 变量引用实现说明

## 这一步解决什么

赋值右侧不再只能写整数常量，也可以读取已经存在的整数变量：

```text
设置 名字 = 1
设置 总数 = 名字
```

## 实现规则

- 标识符出现在赋值右侧时，Parser 生成 `VariableReference`；
- Interpreter 按语句顺序查找变量；
- 变量必须先定义后使用；
- 未定义变量返回带位置的中文执行错误；
- 变量引用不会隐式创建变量，也不会返回默认值。

## 当前没有解决

- 加减乘除；
- 比较和布尔表达式；
- 文字、列表和对象；
- 类型检查；
- 变量作用域和函数参数。
