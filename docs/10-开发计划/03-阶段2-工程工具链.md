# 阶段 2：工程工具链

本阶段在语言核心能够完成真实小项目后开始。重点是让项目更容易使用和协作，而不是盲目增加语言特性。

## 计划范围

- `创建`、`检查`、`运行`、`测试`、`格式化` 等 CLI；
- 多文件模块和本地路径依赖；
- 锁定文件和离线构建；
- VS Code 语法高亮和基础 Language Server；
- 示例项目、用户文档和贡献指南。

## 进入条件

- 语言核心测试稳定；
- 标准库 API 有中文说明；
- 至少一个小型项目由非作者参与完成；
- 已有明确的版本和破坏性变更记录方式。
