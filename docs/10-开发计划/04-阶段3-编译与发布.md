# 阶段 3：编译与发布

这是长期探索阶段，不是当前承诺。只有在语言语义、类型系统和运行时边界稳定后，才评估 Native 或 WASM。

## 可能范围

- 自有 IR；
- 解释器和后端共享语义测试；
- 一个明确目标平台的 Native 后端；
- 可选的 WASM 后端；
- 运行时打包和构建清单；
- 基础调试信息。

## 进入条件

- 已有稳定的值表示、对象布局和错误模型；
- 运行时 ABI 有书面说明；
- 解释器有足够的差分测试；
- 有人愿意长期维护目标平台和发布流程。
