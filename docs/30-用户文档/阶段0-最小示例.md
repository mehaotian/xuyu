# 阶段 0：最小示例

当前语言只支持一条整数赋值语句：

```text
设置 名字 = 1
```

示例文件位于：

```text
examples/阶段0/最小赋值.中
```

当前 CLI 已经可以运行这个文件：

```bash
GOCACHE=/tmp/xu-go-cache go run ./cmd/zhongwen 运行 examples/阶段0/最小赋值.中
```

预期输出：

```text
环境[名字=1]
```

也可以运行完整测试：

```bash
GOCACHE=/tmp/xu-go-cache go test ./...
```
